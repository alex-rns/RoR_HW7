<div>
  <strong>
    <%= comment.author.first_name %> <%= comment.author.last_name %>:
  </strong>
  <%= comment.body %>
  <%= comment.status %>
  <%= link_to 'Delete', post_comment_path(@post, comment), method: :delete, data: { confirm: 'Are you sure you want to delete the comment?' } %>
</div>